{
  "name": "givewell-app",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.9.4"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-router": "next",
    "react-router-dom": "next",
    "redux": "^3.6.0"
  },
  "homepage": "https://johannth.github.io/givewell-app",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp public/404.html build/404.html && cp public/redirect.js build/static/js/redirect.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "rm -rf build && yarn run build",
    "deploy": "gh-pages -d build"
  }
}
